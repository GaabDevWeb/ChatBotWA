# =============================================================================
# ORBITBOT - CONFIGURAÇÕES DE AMBIENTE
# =============================================================================
# Este arquivo contém todas as variáveis de ambiente necessárias para o
# funcionamento do OrbitBot. Mantenha este arquivo seguro e nunca o compartilhe.
#
# IMPORTANTE: Após configurar as variáveis, reinicie o bot para aplicar as mudanças.
# =============================================================================

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE IA - GEMINI (NOVA CONFIGURAÇÃO)
# -----------------------------------------------------------------------------
# Chave de API do Google Gemini (OBRIGATÓRIA)
# Obtenha sua chave em: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# Modelo do Gemini a ser usado (padrão: gemini-2.0-flash-exp)
GEMINI_MODEL=gemini-2.0-flash-exp

# Configurações opcionais do modelo de IA
# Temperatura: controla a criatividade das respostas (0.0 a 2.0)
# Valores menores = respostas mais consistentes
# Valores maiores = respostas mais criativas
GEMINI_TEMPERATURE=0.7

# Máximo de tokens por resposta (padrão: 800)
# Valores maiores permitem respostas mais longas, mas custam mais
GEMINI_MAX_TOKENS=800



# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE TIMEOUT E RETRY
# -----------------------------------------------------------------------------
# Número máximo de tentativas em caso de erro (padrão: 3)
OPENAI_MAX_RETRIES=3

# Timeout inicial em milissegundos (padrão: 8000ms = 8s)
OPENAI_INITIAL_TIMEOUT=2000

# Timeout máximo em milissegundos (padrão: 30000ms = 30s)
OPENAI_MAX_TIMEOUT=8000

# Máximo de mensagens no histórico para contexto (padrão: baseado na configuração)
# Valores maiores mantêm mais contexto, mas consomem mais tokens
OPENAI_MAX_HISTORY=20

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DO SERVIDOR WEB (DASHBOARD)
# -----------------------------------------------------------------------------
# Porta do dashboard web (padrão: 3000)
# Acesse em: http://localhost:DASHBOARD_PORT
DASHBOARD_PORT=3000

# Host do dashboard (padrão: localhost)
# Para acesso externo, use 0.0.0.0 (CUIDADO: apenas em redes seguras)
DASHBOARD_HOST=localhost

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DO BANCO DE DADOS
# -----------------------------------------------------------------------------
# Caminho do banco de dados SQLite (padrão: database/data/orbitbot.db)
# Não altere a menos que saiba o que está fazendo
DATABASE_PATH=database/data/orbitbot.db

# Diretório de backups (padrão: database/backups)
BACKUP_DIR=database/backups

# Intervalo de backup automático em horas (padrão: 24)
# 0 = desabilita backup automático
AUTO_BACKUP_INTERVAL=24

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE LOGGING
# -----------------------------------------------------------------------------
# Nível de log: error, warn, info, debug (padrão: info)
LOG_LEVEL=info

# Salvar logs no banco de dados (true/false)
LOG_TO_DATABASE=true

# Máximo de logs no banco (padrão: 10000)
# Logs mais antigos são removidos automaticamente
MAX_LOG_ENTRIES=10000

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE PERFORMANCE
# -----------------------------------------------------------------------------
# Tamanho máximo do cache em MB (padrão: 50)
CACHE_MAX_SIZE=50

# Tempo de vida do cache em minutos (padrão: 30)
CACHE_TTL=30

# Intervalo de limpeza do cache em minutos (padrão: 10)
CACHE_CLEANUP_INTERVAL=10

# Máximo de mensagens na fila de processamento (padrão: 100)
MAX_QUEUE_SIZE=100

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE SEGURANÇA
# -----------------------------------------------------------------------------
# Lista de administradores (números de telefone separados por vírgula)
# Formato: 5511999999999,5511888888888
# IMPORTANTE: Use o formato internacional completo (código do país + DDD + número)
ADMIN_NUMBERS=

# Modo de desenvolvimento (true/false)
# Em produção, sempre deixe como false
DEV_MODE=false

# Habilitar logs detalhados de API (true/false)
# CUIDADO: pode expor informações sensíveis nos logs
API_DEBUG_LOGS=false

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DO WHATSAPP (BAILEYS)
# -----------------------------------------------------------------------------
# Diretório de autenticação (padrão: auth_info_baileys)
AUTH_DIR=auth_info_baileys

# Timeout de conexão em milissegundos (padrão: 60000ms = 1min)
CONNECTION_TIMEOUT=60000

# Tentativas de reconexão (padrão: 5)
MAX_RECONNECT_ATTEMPTS=5

# Intervalo entre tentativas de reconexão em milissegundos (padrão: 3000ms = 3s)
RECONNECT_INTERVAL=3000

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES AVANÇADAS
# -----------------------------------------------------------------------------
# Habilitar compressão de mensagens (true/false)
MESSAGE_COMPRESSION=true

# Intervalo de monitoramento de performance em segundos (padrão: 60)
PERFORMANCE_MONITOR_INTERVAL=60

# Máximo de conexões simultâneas (padrão: 10)
MAX_CONCURRENT_CONNECTIONS=10

# Timeout para operações de banco em milissegundos (padrão: 5000ms = 5s)
DATABASE_TIMEOUT=5000

# =============================================================================
# INSTRUÇÕES DE CONFIGURAÇÃO
# =============================================================================
#
# 1. CONFIGURAÇÃO MÍNIMA OBRIGATÓRIA:
#    - OPENROUTER_API_KEY: Obtenha em https://openrouter.ai/keys
#    - ADMIN_NUMBERS: Adicione seu número no formato internacional
#
# 2. CONFIGURAÇÃO RECOMENDADA PARA PRODUÇÃO:
#    - DEV_MODE=false
#    - API_DEBUG_LOGS=false
#    - LOG_LEVEL=warn
#    - AUTO_BACKUP_INTERVAL=12 (backup a cada 12 horas)
#
# 3. CONFIGURAÇÃO PARA DESENVOLVIMENTO:
#    - DEV_MODE=true
#    - API_DEBUG_LOGS=true
#    - LOG_LEVEL=debug
#
# 4. SEGURANÇA:
#    - Nunca compartilhe este arquivo
#    - Adicione .env ao .gitignore
#    - Use senhas fortes para APIs
#    - Mantenha backups seguros
#
# 5. PERFORMANCE:
#    - Ajuste CACHE_MAX_SIZE conforme disponibilidade de RAM
#    - Monitore OPENAI_MAX_HISTORY vs custo de tokens
#    - Configure AUTO_BACKUP_INTERVAL conforme necessidade
#
# =============================================================================